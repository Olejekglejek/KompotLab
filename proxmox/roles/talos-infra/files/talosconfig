context: talos-proxmox-cluster
contexts:
    talos-proxmox-cluster:
        endpoints:
            - {{ talos_controlplane_ip }}
        nodes:
            - {{ talos_controlplane_ip }}
        ca: {{ controlplane_ca_crt }}
        crt: {{ kubeconfig_client_crt }}
        key: {{ kubeconfig_client_key }}
