apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-pvc
  namespace: adguard
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: adguard-pv
  resources:
    requests:
      storage: 1Gi
